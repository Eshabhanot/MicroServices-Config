server:
  port: 7077
 
spring:
  datasource:
      url: jdbc:mysql://localhost:3306/userservicedb
      username: root
      password:

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7779/eureka
